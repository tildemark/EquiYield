# Server environment
NODE_ENV=development
PORT=4000

# Admin token for protected routes
ADMIN_TOKEN=changeme-admin-token
JWT_SECRET=super-secret-change-me

# PostgreSQL connection
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/equiyield?schema=public

# Redis connection
REDIS_URL=redis://localhost:6379

# Email (optional; used for password distribution)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@equiyield.local
